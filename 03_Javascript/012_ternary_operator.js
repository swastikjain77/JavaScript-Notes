/*

The ternary operator in JavaScript is a concise way to write an if...else statement. It uses the syntax:
condition ? expressionIfTrue : expressionIfFalse;

let age = 18;
let message = age >= 18 ? "You are an adult." : "You are a minor.";
console.log(message); // Output: "You are an adult."

Here, if age is 18 or more, message is set to "You are an adult." Otherwise, it's set to "You are a minor."


*/


// const age = 2

// age == 3 ? console.log('t') : console.log('f')

// console.log(age)


// let gender = 'F'

// let messageDisplay = `${gender == 'F' ? 'She' : 'He'} is s college student.`

// console.log(messageDisplay)

// let score  = 35

// let grade = score >= 95 ? 'district' : score >= 85 ? 'state' : score >= 60 ? 'just pass' : score >= 35 ? 'legend': 'fail'

// console.log(grade)